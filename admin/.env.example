# API origin (no trailing slash). Defaults to http://localhost:5000 in code if unset.
# VITE_API_URL=http://localhost:5000

# Public marketing site URL (for "View blog" link in admin)
# VITE_PUBLIC_SITE_URL=http://localhost:5173
